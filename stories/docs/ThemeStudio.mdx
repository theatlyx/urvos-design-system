import { Meta } from '@storybook/blocks';
import { TokenPlayground } from './components/TokenPlayground';

<Meta title="Theme & Design/Theme Studio" />

# Theme Studio

Welcome to the Theme Studio. This is a live playground to inspect the design tokens that power the Urvos Design System.

Because all components are wired to these CSS Custom Properties, any changes made to `tokens.css` will immediately reflect across the entire application and here in the playground.

<TokenPlayground />
